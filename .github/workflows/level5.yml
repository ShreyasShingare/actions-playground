name: Level 5 - Job Dependencies

on: push

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Repository
              uses: actions/checkout@v4
            - name: "Build Step"
              run: echo "Building..."
              
    test:
        runs-on: ubuntu-latest
        needs: build
        steps:
            - uses: actions/checkout@v4
            - name: "Test Step"
              run: echo "Testing..."
              